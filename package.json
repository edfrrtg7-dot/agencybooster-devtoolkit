{
  "name": "agencybooster-devtoolkit",
  "version": "0.1.0",
  "description": "AgencyBooster Developer Toolkit - Chrome Extension for development insights",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "dev": "node build.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.21.0",
    "typescript": "^5.4.0"
  }
}
